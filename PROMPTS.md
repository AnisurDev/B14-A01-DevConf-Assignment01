Create a responsive Venue section using clean HTML and vanilla CSS.

Requirements:
- Layout: 2-column grid (text info on left, map placeholder on right). Stacks into 1 column on mobile (<900px).
- Left Content: Centered header, followed by 3 vertical info cards (Location, Date, Accommodation) with light gray backgrounds (#f8fafc), borders, and emojis.
- Right Map Box: A placeholder div with a light gray grid-dot pattern background, containing a centered white overlay card with a bouncing pin animation (📍) and an "Open in Google Maps" button.
- Styling: Keep font sizing clean, colors balanced (#0f172a for titles, #2563eb for blue button).

Provide separate HTML and CSS code without any external frameworks.
















Create a responsive Newsletter section using HTML and raw CSS.

- Background: Dark gradient (#0f172a to #1e293b) with a subtle blue glowing circle behind.
- Desktop: Centered text, inline form inside a glassmorphism border wrapper. Input is transparent, button is blue (#2563eb) with hover and active click scale effects.
- Mobile (<600px): Elements stack vertically and become 100% full-width.

Provide separate HTML and CSS. No frameworks.